import styled from "styled-components";

const Main = styled.div`
    width: 100%;
    background-color: var(--color-menu);
    height: 1px;
    display: flex;
    position: fixed;
    bottom: 0;
`;

export {Main};